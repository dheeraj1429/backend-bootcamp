version: "3.8"

services:
  seq:
    image: datalust/seq:latest
    container_name: seq
    environment:
      - ACCEPT_EULA=y
      - SEQ_PASSWORD=seq_password
    ports:
      - 5341:80
    volumes:
      - seq_data:/data

volumes:
  seq_data: